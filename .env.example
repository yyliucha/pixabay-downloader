# Pixabay API Key 配置模板
#
# 使用步骤:
#   1. 复制本文件为 .env:  cp .env.example .env   (Windows: copy .env.example .env)
#   2. 免费注册账号:  https://pixabay.com/accounts/register/
#   3. 登录后在 https://pixabay.com/api/docs/ 页面复制你的 API key
#   4. 把 key 填到下面等号后面(不要加引号)
#
# 注意: .env 已被 .gitignore 排除, 不会提交到 git, 请勿把真实 key 写进本文件。
PIXABAY_API_KEY=
